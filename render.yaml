services:
  - type: web
    name: telco-churn-prediction-mlops
    runtime: python
    buildCommand: pip install -r requirements_api.txt
    startCommand: uvicorn serving.churn_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
